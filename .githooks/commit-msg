#!/usr/bin/env bash
# Content-triggered Red -> Green TDD commit-msg gate. Installed by
# `bun run bootstrap` via core.hooksPath (see .githooks/README.md).
set -euo pipefail
hook_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bun "$hook_dir/../scripts/tdd-commit-msg-hook.ts" "$1"
